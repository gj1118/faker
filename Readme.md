Faker is a generator of fake artifacts (read files, registry entries, firewall entries)

It should not be used for bad activities, this program is meant to be used to stress test the system and/or setting the system up for security solutions.
